# פרומפט עיצוב — Tehila Bodyshop CRM
> פרומפט מוכן להדבקה בכלי עיצוב AI (Claude / Stitch / v0 / Figma AI). מתאר את כל המערכת, התפקידים, המסכים, וכיוון העיצוב. עברית/RTL, רספונסיבי למובייל + אייפד + דסקטופ.

---

## הקשר

עצב מחדש את ה-UI של **Tehila Bodyshop CRM** — מערכת ניהול תיקי תיקון רכב למוסך פחחות "תהילה" (שני סניפים: נתיבות, אשקלון). המערכת **מונעת על-ידי צ'קליסט**: המשתמש לא משנה סטטוס ידנית — כל שלב מתקדם בלחיצת "סמן בוצע" בלבד. כל המערכת בעברית, **RTL מלא**.

**Stack:** Next.js 14 (App Router) · TailwindCSS · עברית/RTL. שמור על שמות שדות וקופי בעברית.

## הסצנה (קובעת את העיצוב)
מנהל שירות בודק סטטוס תיק על **אייפד** במשרד מואר של מוסך; פחח בודק חלקים על **טלפון** מתחת לתאורת פלורוסנט במוסך, לפעמים עם ידיים מלוכלכות; בעל המוסך (CEO) מאשר אומדנים מהטלפון בדרכים. → **בהירות לפני "מגניבות"**: theme בהיר, ניגודיות גבוהה, יעדי-מגע גדולים (≥44px), סריקה מהירה של "מה השלב הבא".

## המשתמשים (5 תפקידים)
| תפקיד | מי | מה רואה/עושה |
|---|---|---|
| **SERVICE_MANAGER** (ערן) | מנהל סניף | מנהל את ה-workflow המקצועי (13 שלבים): FixCar, אומדן, שמאי, כניסה לעבודה, בקרת איכות, שטיפה |
| **OFFICE** (אילנה) | משרד | סגירה אדמיניסטרטיבית: מסמכים, פרופורמה, טפסי סגירה |
| **CEO** (עמית) | בעלים | **גישה לכל הסניפים** — אישורים, מחיקת תיקים, הגדרות, מקבל את כל ההתראות |
| **PAINTER** (ארז) | פחח | צ'קליסט פחח (נכנס לעבודה, קבלת חלקים), בקשות תוספות |
| **SERVICE_ADVISOR** (כנרת) | יועצת | צפייה בלבד + מקבלת התראות פחח |

הניווט והעמודים **משתנים לפי תפקיד**. CEO רואה הכל; כל שאר התפקידים מוגבלים לסניף שלהם.

## המסכים לעיצוב
1. **התחברות** — מינימלי, לוגו, אימייל+סיסמה, RTL.
2. **רשימת תיקים** (`/cases`) — הלב התפעולי. כל תיק: מספר רישוי, שם לקוח, חברת ביטוח, **השלב הפעיל הנוכחי** (הכי בולט), סטטוס כללי, סניף. סינון/חיפוש מהיר. כפתור "תיק חדש". במובייל: רשימת כרטיסים נגישים לאגודל; באייפד/דסקטופ: טבלה צפופה יותר.
3. **פרטי תיק** (`/cases/[id]`) — **המסך החשוב ביותר**. מציג:
   - **צ'קליסט ה-workflow** — רצף 13 שלבים. כל שלב במצב: ✅ בוצע / 🔵 פעיל / ⚪ ממתין / ⏭️ דולג. **השלב הפעיל חייב להיות בלתי-נמנע לעין**, עם הפעולה הראשית **"סמן בוצע"**. חלק מהשלבים דורשים קישור/קובץ לפני השלמה (FixCar, גלגלים, אומדן).
   - פרטי תיק ורכב (עריכה inline), מסמכים (העלאה + מצלמה), אישורי CEO, יומן אודיט ("בוצע על ידי X").
   - חסימות מוצגות בבירור (למשל "מוכן למשרד" חסום עד אישור CEO).
4. **אישורים** (`/approvals`, CEO) — תור אישורים ממתינים. כל פריט: רכב, לקוח, סוג אישור, הערות. פעולות **אשר / דחה** (דחייה עם הערה).
5. **סגירה** (`/closure`, OFFICE+CEO) — צ'קליסט סגירה בן 4 שלבים.
6. **פחחים** (`/painters`, `/painters/[id]`) — ממשק פחח נפרד ופשוט: צ'קליסט קצר (נכנס לעבודה / התקבלו חלקים), בקשת תוספת עם צילום. אופטימלי לטלפון.
7. **התראות** (`/notifications`) + **פעמון** בכל מסך — חלון נפתח מהפעמון עם רשימת התראות; לחיצה על התראה → מעבר ישיר לתיק/אישור. מציג "נשלח על ידי X", deep-link, מצב נקרא/לא-נקרא.
8. **הגדרות** (`/settings`, CEO) — מטריצת הרשאות, עריכת שלבי workflow, ניהול יועצי פחח. טאבים.

## שפה ויזואלית
- **Theme:** בהיר. נייטרלים **חמימים** (לא לבן/שחור טהור — תייג כל אפור קלות לכיוון גוון המותג). השתמש ב-OKLCH.
- **צבע:** אסטרטגיה **מאופקת** — נייטרלים + accent אחד בטוח. אבל **מערכת סטטוס סמנטית חזקה וברורה** היא קריטית: לכל מצב צ'קליסט וסטטוס תיק צבע מובחן שנסרק מיידית (בוצע/פעיל/ממתין/דולג/חסום/נדחה). הסטטוסים הם ליבת ה-UX, לא קישוט.
- **טיפוגרפיה:** גופן עברי איכותי (Heebo / Assistant / Rubik / Ploni). היררכיה דרך גודל+משקל (יחס ≥1.25 בין דרגות). מספרי רישוי ומק"טים — קריאים מאוד.
- **פריסה:** מנע רשת-כרטיסים אחידה ומשעממת. ה-flow של הצ'קליסט הוא ציר אנכי ברור עם זרימה RTL. ריווח משתנה לקצב, לא padding זהה בכל מקום.
- **תנועה:** עדינה ותכליתית. ease-out מעריכי. בלי bounce. אל תנפיש מאפייני layout.

## רספונסיביות (חובה)
- **מובייל (טלפון):** עמודה אחת, כרטיסים גדולים נגישים לאגודל, פעולה ראשית ברורה ("סמן בוצע" כפתור רחב). ניווט תחתון/המבורגר. שדות קובץ עם אפשרות **מצלמה ישירה**.
- **אייפד (טאבלט):** שימוש בשטח רחב יותר — רשימת תיקים + פרטי תיק זה לצד זה (master-detail) כשאפשר, טבלאות צפופות, צ'קליסט וקופץ-מידע נוחים למגע. **זה המכשיר העיקרי בעבודה — תן לו תשומת לב מיוחדת.**
- **דסקטופ:** ניצול רוחב מלא, צפיפות מידע גבוהה יותר.
- כל יעדי-המגע ≥44px. RTL נכון: אייקונים, חצים, וזרימת התקדמות מימין לשמאל.

## נגישות
ניגודיות גבוהה (סביבת מוסך + משתמשים מבוגרים), טקסט גדול דיו, מצבי focus ברורים, לא להסתמך על צבע בלבד למצב (הוסף אייקון/תווית).

## אל תעשה (anti-slop)
- ❌ בלי "AI slop": רשת כרטיסים זהים אינסופית, גרדיאנט-טקסט, פסי-צד צבעוניים (border-left/right ככותרת), זכוכית מטושטשת דקורטיבית, תבנית "מספר ענק + תווית" (hero-metric), modal כמחשבה ראשונה (העדף inline/progressive).
- ❌ בלי כחול-SaaS גנרי כברירת מחדל, ובלי "מוסך = כתום/שחור תעשייתי" קלישאתי. מצא כיוון מבוגר ורגוע יותר.
- ❌ בלי em-dash בקופי.

## הפלט הרצוי
מסכים רספונסיביים (מובייל/אייפד/דסקטופ) לכל העמודים שלמעלה, עם דגש על **רשימת תיקים** ו**פרטי תיק (הצ'קליסט)**. כל הקופי בעברית, RTL מלא. ספק גם מערכת design tokens (צבעי סטטוס, טיפוגרפיה, ריווח) לשימוש חוזר ב-Tailwind.
