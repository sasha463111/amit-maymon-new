# מערכת תהילה — סקירת מערכת מלאה

> מסמך ייחוס. מתאר מה המערכת עושה, איך היא בנויה, ומה מצבה נכון ל‑20.09.2026.
> כל הנתונים נשלפו מהמערכת החיה, לא מתיעוד.

---

## 1. מה זו המערכת

מערכת לניהול מוסך פחחות וצבע — מלווה רכב מרגע שלקוח פונה ועד סגירת התיק מול חברת הביטוח.

| | |
|---|---|
| **לקוח** | מוסך תהילה (טויוטה), עמית מימון — מנכ״ל |
| **סניפים** | נתיבות, אשקלון |
| **משתמשים** | 8 פעילים בפרודקשן |
| **כתובת** | amit-maymon-new.vercel.app |
| **היקף קוד** | ~21,000 שורות TypeScript |

### שתי ישויות מרכזיות

**הפנייה** — לקוח פוטנציאלי. נרשם לפני שהוחלט אם ייפתח תיק. יש לה מעקב סטטוסים ("ממתין לרכב חלופי", "ממתין לניירת"), ואפשר להמיר אותה לתיק.

**תיק** — עבודה בפועל על רכב. עובר תהליך מובנה של 13 שלבים, ואז תהליך סגירה נפרד.

---

## 2. ארכיטקטורה

| שכבה | טכנולוגיה |
|---|---|
| ממשק | Next.js 14 (App Router), React Server Components |
| שרת | Server Actions — אין REST API נפרד |
| בסיס נתונים | PostgreSQL בענן (Supabase) |
| אבטחה | Row Level Security — נאכפת בבסיס הנתונים |
| קבצים | Supabase Storage, 4 דליים פרטיים |
| התראות | טבלה פנימית + Web Push (VAPID) |
| אירוח | Vercel, פריסה אוטומטית מ‑main |
| משימות מתוזמנות | GitHub Actions, כל 30 דקות |

### עקרון מנחה

**האבטחה יושבת בבסיס הנתונים, לא בקוד.** כל טבלה מוגנת ב‑RLS, כך שגם אם קוד שוגה או מישהו פונה ישירות ל‑API, ההרשאות נאכפות. הקוד הוא שכבה שנייה — הוא מונע פעולה מוקדם ומציג שגיאה ברורה, אבל אינו השומר היחיד.

---

## 3. מבנה הנתונים — 24 טבלאות

### ליבה

| טבלה | שורות | תפקיד |
|---|---|---|
| `cases` | 63 | תיקי עבודה. 43 עמודות — הטבלה הרחבה במערכת |
| `referrals` | 34 | הפניות (לקוחות פוטנציאליים) |
| `cars` | 66 | רכבים. תיק מקושר לרכב, רכב יכול לחזור |
| `profiles` | 19 | משתמשים, תפקידים ושיוך לסניפים |
| `branches` | 2 | נתיבות, אשקלון |

### תהליך העבודה

| טבלה | שורות | תפקיד |
|---|---|---|
| `case_workflow_runs` | 83 | מופע תהליך לתיק (מקצועי או סגירה) |
| `case_workflow_steps` | 899 | השלבים בפועל ומצבם |
| `workflow_step_templates` | 13 | הגדרת השלבים — ניתנת לעריכה מההגדרות |
| `ceo_approvals` | 45 | בקשות הדורשות אישור מנכ״ל |

### פחחות

| טבלה | שורות | תפקיד |
|---|---|---|
| `painter_requests` | 53 | בקשות מהפחח למנהל השירות |
| `painter_request_images` | — | תמונות מצורפות לבקשה |
| `bodywork_extras` | 25 | עבודות נוספות שהתגלו תוך כדי |

### מסמכים

| טבלה | שורות | תפקיד |
|---|---|---|
| `case_documents` | 42 | מסמכי תיק — שמאי, ביטוח, טפסים |
| `referral_documents` | 11 | מסמכים שצורפו להפנייה |

### התראות

| טבלה | שורות | תפקיד |
|---|---|---|
| `notifications` | 2,902 | התראות בתוך האפליקציה (הפעמון) |
| `push_subscriptions` | 17 | מכשירים רשומים לקבלת התראות דחיפה |

### ניהול וביקורת

| טבלה | שורות | תפקיד |
|---|---|---|
| `role_permissions` | 45 | **מטריצת ההרשאות** — 5 תפקידים × 9 פעולות |
| `audit_events` | 732 | יומן פעולות |
| `audit_log`, `activity_log` | — | יומנים נוספים |
| `system_messages` | 1 | הודעת מערכת לכל המשתמשים |
| `insurance_branch_mapping` | — | סינון סניפים לפי חברת ביטוח |
| `schema_migrations` | 51 | מעקב מיגרציות |

### מפת קשרים

```
branches ──┬── cases ──┬── cars
           │           ├── case_workflow_runs ── case_workflow_steps
           │           ├── ceo_approvals
           │           ├── painter_requests ── painter_request_images
           │           ├── bodywork_extras
           │           ├── case_documents
           │           └── notifications
           │
           └── referrals ──┬── referral_documents
                           ├── referral_status_updates
                           └── (case_id — לאחר המרה לתיק)

profiles ── כל טבלה מקשרת אליה דרך created_by / uploaded_by / user_id
```

---

## 4. התהליך העסקי

### תהליך מקצועי — 13 שלבים

השלבים מוגדרים בטבלה וניתנים לעריכה מההגדרות, בלי שינוי קוד.

| # | שלב | הערה |
|---|---|---|
| 1 | פתיחת תיק | |
| 2 | צילום FixCar | |
| 3 | טפסי גלגלים | |
| 4 | אומדן | |
| 5 | שליחה לשמאי | |
| 6 | המתנה לאישור שמאי | **דורש אישור מנכ״ל** |
| 7 | כניסה לעבודה | מפעיל תזכורות לפחחים |
| 8 | ניפוק מק״טים | |
| 9 | הנחות חלקים | |
| 10 | בקרת איכות | |
| 11 | שטיפה | |
| 12 | שליחת תמונות לשמאי גמר תיקון | |
| 13 | מוכן למשרד | מעביר את התיק למשרד |

### תהליך סגירה

נפתח אוטומטית כשהתיק מגיע ל"מוכן למשרד":

1. אימות פרטים ומסמכים
2. פרופורמה (אם נדרש)
3. הכנת טפסי סגירה

---

## 5. מודל ההרשאות

### חמישה תפקידים

| תפקיד | מי | תחום אחריות |
|---|---|---|
| **מנכ״ל** | עמית מימון | הכל, בכל הסניפים |
| **משרד** | אילנה, אילנית, אביה | הפניות, פתיחת תיקים, מסמכים, סגירה |
| **מנהל שירות** | ערן | תיקים, קידום התהליך, תוספות, מענה לפחחים |
| **יועצת שירות** | נסיה | קידום התהליך, מענה לפחחים |
| **פחח** | פחחים נתיבות/אשקלון | צפייה, תמונות, הגשת בקשות |

### מטריצת ההרשאות

מאז 18.09.2026 טבלת `role_permissions` היא **מקור האמת היחיד** — נאכפת בשלוש שכבות: ממשק, שרת ובסיס נתונים. כל תא ניתן לשינוי מההגדרות.

| פעולה | מנכ״ל | משרד | מנהל שירות | יועצת שירות | פחח |
|---|:---:|:---:|:---:|:---:|:---:|
| פתיחת הפנייה | ✔ | ✔ | ✖ | ✖ | ✖ |
| פתיחת תיק | ✔ | ✔ | ✔ | ✖ | ✖ |
| קידום שלב מקצועי | ✔ | ✖ | ✔ | ✔ | ✖ |
| קידום שלב סגירה | ✔ | ✔ | ✖ | ✖ | ✖ |
| אישור בקשות חריגות | ✔ | ✖ | ✖ | ✖ | ✖ |
| ניהול תוספות | ✔ | ✖ | ✔ | ✖ | ✖ |
| העלאת מסמכים | ✔ | ✔ | ✔ | ✔ | ✖ |
| מחיקת מסמכים | ✔ | ✔ | ✔ | ✖ | ✖ |
| כניסה להגדרות | ✔ | ✖ | ✖ | ✖ | ✖ |

### ארבעה כללים שאינם ניתנים לשינוי

1. **המנכ״ל לעולם אינו מוגבל** — הרשאותיו פתוחות תמיד ואינן ניתנות לעריכה. אי אפשר לנעול את הבעלים מחוץ למערכת שלו.
2. **הפרדת סניפים קבועה בקוד** — הטבלה קובעת *מה* מותר לתפקיד, לעולם לא *באיזה סניף*.
3. **בדיקה שנכשלת — חוסמת** — אם בדיקת הרשאה לא מצליחה לרוץ, הפעולה נחסמת.
4. **הרשאה מוענקת בכל השכבות או בכלל לא** — הרשאה חלקית גרועה מכלום.

### חריגים מכוונים

| נושא | מדיניות |
|---|---|
| ניהול משתמשים | מנכ״ל בלבד, לא ניתן להאצלה |
| תמונות עבודה ותוספות | כל מי שעובד על תיק בסניפו — כולל פחח |
| מחיקת קובץ שהעלית | תמיד מותר |
| בקשות פחחים | פחח מגיש; מנהל שירות, יועצת שירות ומנכ״ל משיבים |

---

## 6. מערכת ההתראות

### שני ערוצים נפרדים

| | בתוך האפליקציה | דחיפה למכשיר |
|---|---|---|
| נשמר ב | `notifications` | `push_subscriptions` |
| מי מקבל | כולם | רק מי שאישר בדפדפן |
| נראה | בפעמון | קופץ גם כשהמערכת סגורה |

**הערה חשובה:** התראת דחיפה דורשת אישור חד־פעמי בדפדפן של המשתמש. לא ניתן להפעיל אותה מהשרת.

### ניתוב

12 סוגי אירועים, כל אחד מנותב לתפקידים הרלוונטיים בסניף שבו קרה האירוע. המנכ״ל מקבל הכל — כמסלול ביקורת.

### תזכורות אוטומטיות

רצות כל 30 דקות דרך GitHub Actions:

| תזכורת | תנאי |
|---|---|
| כניסה לעבודה | רכב נכנס לעבודה ואיש לא אישר, כל ~2 שעות בשעות העבודה |
| הסלמת בקשת פחח | בקשה ממתינה מעל שעה |
| מסירה למשרד | תיק מוכן לסגירה מעל שעה |
| מעקב הפניות | תאריך מעקב שהגיע |

---

## 7. מה תוקן — ביקורת 18–20.09.2026

15 תקלות בפרודקשן, כולן תוקנו ונפרסו.

### השורש המשותף

עמודת `branch_id` (סניף בודד) הוחלפה במערך `branch_ids` (ריבוי סניפים). חלק מהמקומות לא עודכנו — וכיוון ששאילתה לעמודה שאינה קיימת **נכשלת** במקום להחזיר ריק, כל אחד מהם נפל בשקט ל"אסור" או "ריק".

### התקלות

| # | תקלה | השפעה |
|---|---|---|
| 1 | השלמת שלב בתהליך | **אף אחד**, כולל מנכ״ל, לא יכול היה להתקדם בתהליך |
| 2 | פונקציית נמעני ההתראות | **כל ההתראות מתו 17 יום** + כל 4 התזכורות |
| 3–4 | עדכוני מעקב בהפניות | עובד רב־סניפי נחסם בסניף השני |
| 5 | צפייה בפרופילים | כל עובד ראה רק את עצמו → בורר יועצים ריק, שמות חסרים ביומן |
| 6 | חסימת יועצת מעמוד הפחחים | נסיה קיבלה **98 התראות** שלא יכלה לפתוח |
| 7 | "צפייה כמשתמש" של המנכ״ל | החזיר "משתמש לא נמצא" תמיד |
| 8 | רשימת יועצי פחחות | לשונית ההגדרות הופיעה ריקה |
| 9 | סנכרון הרשאות בזמן אמת | נפרס ומעולם לא פעל |
| 10 | סינון לפי חברת ביטוח | מעולם לא פעל |
| 11 | קוד בדיקה בפרודקשן | מזהה משתמש ופרופיל נרשמו ליומן בכל טעינה |
| 12 | תמונות פחחים ותוספות | **אף אחד** לא יכול היה להעלות או לצפות |
| 13 | **דליפת מידע** | גולש לא מחובר שלף רשימת **17 עובדים** |
| 14 | הודעות מערכת | הוגשו לכל אדם ברשת |
| 15 | 8 אינדקסים חסרים | האטה שגדלה עם הנתונים |

### שיפורים מבניים

- **מטריצת ההרשאות חוברה לאכיפה אמיתית** — הייתה תצוגה בלבד
- **"פתיחת הפנייה" נוספה כפעולה תשיעית** — לא הייתה ניתנת להענקה כלל
- **גישה אנונימית לכל פונקציות ה‑API בוטלה**

---

## 8. מה נדרש

### לפני הכנסת משתמשים חדשים

**הגנה מסיסמאות דלופות** — Supabase יכולה לבדוק סיסמה חדשה מול מאגר סיסמאות שדלפו ולמנוע שימוש בהן. כרגע **כבויה**. הפעלה בלוח הבקרה של Supabase, לא ניתן מהקוד.

### גיבוי — ממתין להחלטה

הגיבוי היומי נכשל מאז 04.09. שתי סיבות: הסיסמה לבסיס הנתונים לא הוגדרה כסוד במאגר, והכתובת הצביעה על שרת ה‑API. התהליך תוקן אך **לא ירוץ עד שתתווסף הסיסמה**.

הפרויקט בתוכנית החינמית שאינה כוללת גיבוי מנוהל.

| אפשרות | חודשי | מה מקבלים |
|---|---|---|
| נוכחי (חינם) | $0 | אין גיבוי |
| תיקון הייצוא העצמי | $0 | הוספת סיסמה; נשמר 30 יום ב‑GitHub |
| Supabase Pro | $25 | גיבוי יומי, שחזור 7 ימים אחורה |
| Pro + שחזור לנקודת זמן | $125+ | שחזור לדקה מדויקת |

> **גיבוי בסיס הנתונים אינו כולל קבצים שהועלו** — בכל התוכניות. תמונות ומסמכים דורשים פתרון נפרד.

### סיכון טכני למעקב

**81 שאילתות בקוד אינן בודקות אם נכשלו.** הן עובדות כיום, אבל זה בדיוק הדפוס שייצר את רוב התקלות: השאילתה נכשלת בשקט, התוצאה חוזרת ריקה, וההרשאה נופלת ל"אסור". לא דחוף, אבל זו נקודת התורפה המבנית.

### מומלץ לפני לייב

- ליצור משתמש בדיקה דרך המסך ולוודא את הנתיב המלא — מייל, התחברות ראשונה, הרשאות
- לבקש מכל עובד לאשר התראות בדפדפן (אילנה למשל עדיין ללא מכשיר רשום)
- לאשר את מסמך ההרשאות מול עמית

---

## 9. חוב טכני ידוע

אף אחד מהפריטים כאן אינו תקלה. המערכת עובדת. כל פריט מתועד עם **הטריגר** שיגיד מתי כדאי לטפל — כדי שלא ילך לאיבוד, ובלי לחייב פעולה עכשיו.

### 9.1 טבלת `cases` — דפוס "עמודה לכל שלב"

43 עמודות. המספר עצמו אינו הבעיה — הבעיה היא שדפוס אחד חוזר ארבע פעמים, ומייצר 11 עמודות:

| הדפוס | העמודות |
|---|---|
| מי אחראי לשלב | `qc_assignee`, `catalog_numbers_assignee`, `parts_discounts_assignee`, `completion_photos_assignee` |
| קישור לשלב | `fixcar_link`, `wheels_check_link`, `estimate_link` |
| תזכורת נשלחה | `painter_reminder_sent_at`, `office_reminder_sent_at` |
| מצב צ'קליסט | `closure_checklist_state`, `enter_work_checklist_state` |

**הסתירה:** קיימת טבלת `case_workflow_steps` (899 שורות) שמנהלת בדיוק את השלבים האלה. המידע *על* השלב יושב בטבלה, אך המטא־דאטה שלו יושבת כעמודות על `cases`.

**המשמעות:** שלב חדש בתהליך דורש מיגרציה ועמודות חדשות, במקום שורה.

> **טריגר:** כשתידרש הוספת שלב לתהליך, ותגלה שזה מחייב מיגרציה במקום שורה במסך ההגדרות.
>
> **התיקון:** להעביר `assignee` / `link` / `reminder_sent_at` להיות עמודות על `case_workflow_steps`. 11 עמודות יורדות ל‑0, ושלב חדש הופך לשורה.

**לא למחוק:** `estimate_link`, `painter_status_other_text` ו‑`sub_claim_type_other_text` ריקות בכל 63 התיקים — אך כולן בשימוש פעיל בקוד (8–19 הפניות). הן פיצ'רים שטרם נוצלו, לא שאריות.

### 9.2 שתי מערכות מיגרציה במקביל

| תיקייה | קבצים | מצב | נמעקב ב |
|---|---|---|---|
| `supabase/migrations/` | 27 | **החיה** | `supabase_migrations.schema_migrations` |
| `src/db/migrations/` | 49 | **מתה** | `public.schema_migrations` |

`src/db/` אינו מוזכר באף מקום בקוד האפליקציה (0 הפניות). זו היסטוריה מלפני המעבר ל‑Supabase CLI.

**הסיכון אמיתי ולא תיאורטי:** במהלך ביקורת 09.2026 חיפוש אחר מדיניות אבטחה החזיר תוצאות מ‑`src/db/migrations/017_soft_delete_and_painter.sql` — קובץ מת — והוביל לרגע של בלבול לגבי מה באמת פעיל. מפתח חדש עלול לערוך את הקבצים הלא נכונים ולתהות למה שום דבר לא משתנה.

> **טריגר:** בפעם הבאה שמישהו מחפש מדיניות או סכמה ומגיע לקובץ הלא נכון.
>
> **התיקון:** להעביר את `src/db/` ל‑`docs/legacy-migrations/` עם README שמסביר שזו היסטוריה בלבד, או למחוק (הכל בהיסטוריית git). בנוסף להסיר את טבלת `public.schema_migrations` המתה.

### 9.3 שלוש טבלאות ביקורת, אחת בשימוש

| טבלה | שורות | מצב |
|---|---|---|
| `audit_events` | 732 | **פעילה** — מתעדכנת |
| `audit_log` | 0 | ריקה. יש הפניה בקוד אך איש אינו כותב אליה |
| `activity_log` | 0 | ריקה. אותו מצב |

> **טריגר:** אם תידרש היסטוריית פעולות מפורטת יותר — להחליט אם להחיות אחת מהן או להרחיב את `audit_events`.
>
> בינתיים: לא מזיק, רק מבלבל בקריאת הסכמה.

### 9.4 סינון סניפים לפי חברת ביטוח — פיצ'ר מת

`insurance_branch_mapping` **ריקה לחלוטין**. הקוד (`getFilteredBranches`) נופל ל־fallback שמחזיר את כל הסניפים, ולכן הפיצ'ר מעולם לא השפיע על דבר.

מדיניות ה‑RLS על הטבלה תוקנה ב‑18.09 (היא הייתה מוגנת ללא אף כלל, כך שכל קריאה חזרה ריקה) — אך **תיקון ההרשאה אינו מספיק כל עוד אין נתונים בטבלה**.

> **טריגר:** אם עמית ירצה שטופס פתיחת הפנייה יציג רק סניפים שעובדים עם חברת הביטוח שנבחרה.
>
> **הנדרש:** לאכלס את הטבלה במיפוי האמיתי. עד אז — הטופס מציג את שני הסניפים תמיד, וזו התנהגות סבירה.

### 9.5 ריבוי מסמכים

20 קבצי Markdown בשורש, חלקם מפברואר 2026 ומתארים מצב שאינו קיים עוד:

| קובץ | עודכן | הערכה |
|---|---|---|
| `SPEC.md`, `PREVIEW.md`, `CHAT_HISTORY.md` | 02.2026 | מיושנים |
| `DESIGN_PROMPT.md` + `CLAUDE_DESIGN_PROMPT.md` | 08.2026 | כפילות |
| `SESSION_SUMMARY.md` | 08.2026 | תמונת רגע |
| `NOTIFICATIONS_AUDIT_COMPLETE.md`, `NOTIFICATION_REFACTOR_REPORT.md` | 09.2026 | חופפים ל‑`NOTIFICATIONS_ROUTING.md` |

**הסיכון:** תיעוד סותר מסוכן יותר מהיעדר תיעוד — קוראים בו ומאמינים לו.

> **טריגר:** כשמישהו יסתמך על מסמך מיושן ויטעה.
>
> **התיקון:** המסמכים החיים הם `CLAUDE.md`, `SYSTEM_OVERVIEW.md`, `BUSINESS_PROCESS.md`, `NOTIFICATIONS_ROUTING.md` ו‑`AUDIT-2026-09-18.md`. השאר ל‑`docs/archive/`.

### 9.6 כפילות פרטי לקוח

שם הלקוח והטלפון נשמרים גם ב‑`cases` וגם ב‑`referrals`, ללא טבלת לקוחות.

מצב נוכחי: 10 לקוחות מופיעים בשתי הטבלאות, 4 לקוחות ביותר מתיק אחד, 0 רכבים ביותר מתיק אחד. בהיקף הזה הכפילות אינה כואבת.

> **טריגר:** בקשה להיסטוריית לקוח — "כמה פעמים הרכב הזה היה אצלנו", "מה עשינו ללקוח הזה בעבר".
>
> **התיקון:** טבלת `customers` אמיתית, ו‑`cases`/`referrals` מקשרות אליה.

### 9.7 שאילתות ללא בדיקת שגיאה

81 שאילתות ב‑Server Actions אינן בודקות אם נכשלו. הן עובדות כיום — אך זה בדיוק הדפוס שייצר את רוב תקלות ביקורת 09.2026: השאילתה נכשלת בשקט, `data` חוזר ריק, ובדיקת ההרשאה נופלת ל"אסור" בלי שאיש יראה שגיאה.

> **טריגר:** שינוי סכמה נוסף. זהו מכפיל הנזק שהפך שגיאת עמודה אחת ל‑12 תקלות נפרדות.
>
> **התיקון:** בעת נגיעה ב‑Server Action, להוסיף בדיקת `error` — ולא לעשות זאת ב‑81 מקומות בבת אחת.

---

## 10. תפעול

### פריסה

דחיפה ל‑`main` מפעילה פריסה אוטומטית ב‑Vercel. מיגרציות מוחלות בנפרד ב‑`npx supabase db push --linked`.

### חוקים קבועים בפרויקט

מתועדים ב‑`CLAUDE.md`:

| # | חוק |
|---|---|
| 1 | פריסה אוטומטית לפרודקשן לאחר build מוצלח |
| 2 | תיעוד מתעדכן באותה דחיפה כמו הקוד |
| 3 | ניתוב התראות לפי תפקיד |
| 4 | בדיקות QA כוללות מסלול כושל, לא רק מוצלח |
| 5 | הרשאות חיות בטבלה, לעולם לא בקוד |
| 6 | אימות הרשאות בהתחזות, לא בהיסק |

### כיצד מאמתים הרשאה

לא די לקרוא את הכלל ולהסיק. יש להריץ את הפעולה בזהות המשתמש:

```sql
BEGIN;
SET LOCAL ROLE authenticated;
SET LOCAL request.jwt.claims = '{"sub":"<user-uuid>","role":"authenticated"}';
-- הפעולה האמיתית כאן
ROLLBACK;  -- תמיד
```

---

## 11. מסמכים נוספים

| קובץ | תוכן |
|---|---|
| `CLAUDE.md` | מדריך מלא לפיתוח, סכמה, חוקים קבועים |
| `AUDIT-2026-09-18.md` | פירוט הביקורת וכל התקלות |
| `SYSTEM_OVERVIEW.md` | המסמך הזה — כולל פרק חוב טכני |
| `NOTIFICATIONS_ROUTING.md` | ניתוב התראות ותיעוד התקריות |
| `BUSINESS_PROCESS.md` | התהליך העסקי |

---

*נכון ל‑20.09.2026. כל הנתונים נשלפו מהמערכת החיה.*
